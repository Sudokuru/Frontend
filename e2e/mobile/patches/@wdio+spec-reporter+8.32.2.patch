diff --git a/node_modules/@wdio/spec-reporter/build/index.js b/node_modules/@wdio/spec-reporter/build/index.js
index 888012f..fda3710 100644
--- a/node_modules/@wdio/spec-reporter/build/index.js
+++ b/node_modules/@wdio/spec-reporter/build/index.js
@@ -517,7 +517,7 @@ export default class SpecReporter extends WDIOReporter {
         }
         const caps = (capability.alwaysMatch ||
             capability);
-        const device = caps['appium:deviceName'];
+        const device = caps.deviceName || caps['appium:deviceName'];
         const app = ((caps['appium:app'] || caps.app) || '').replace('sauce-storage:', '');
         const appName = app || caps['appium:bundleId'] || caps.bundleId;
         const browser = caps.browserName || caps.browser || appName;
@@ -528,7 +528,7 @@ export default class SpecReporter extends WDIOReporter {
          * platformVersion: mobile format
          * browser_version: invalid BS capability
          */
-        const version = caps.browserVersion || caps.version || caps['appium:platformVersion'] || caps.browser_version;
+        const version = caps.browserVersion || caps.version || caps.platformVersion || caps['appium:platformVersion'] || caps.browser_version;
         /**
          * fallback to different capability types:
          * platformName: W3C format
